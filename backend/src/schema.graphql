# type Query {}

# type Mutation {}

type User {
  id: ID!
  name: String
}

type Item {
  id: ID!
  title: String!
  description: String!
  price: Int!
  image: String
  largeImage: String
  # user: User!
}
