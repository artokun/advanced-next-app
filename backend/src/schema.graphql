scalar DateTime

type Query {
  items: [Item]!
}

type Mutation {
  createItem(
    title: String
    description: String
    price: Int
    image: String
    largeImage: String
    createdAt: DateTime
    updatedAt: DateTime
  ): Item
}

type User {
  id: ID!
  name: String
}
