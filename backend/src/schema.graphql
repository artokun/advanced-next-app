type Query {
  users: [User]!
}

type Mutation {
  updateName(id: ID!, name: String!): User!
}

type User {
  id: ID!
  name: String!
  email: String!
}

type Item {
  id: ID!
  title: String!
  description: String!
  price: Int!
  image: String
  largeImage: String
  # user: User!
}
